# ============================================
# SCANEAT Android App - 설정 가이드
# ============================================

## 📱 Android Studio 프로젝트 설정

### 1. build.gradle (Module: app)

```gradle
plugins {
    id 'com.android.application'
    id 'org.jetbrains.kotlin.android'
}

android {
    namespace 'com.example.scaneat'
    compileSdk 34

    defaultConfig {
        applicationId "com.example.scaneat"
        minSdk 24
        targetSdk 34
        versionCode 1
        versionName "1.0"
    }

    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android-optimize.txt'), 'proguard-rules.pro'
        }
    }
    
    buildFeatures {
        viewBinding true
    }
    
    compileOptions {
        sourceCompatibility JavaVersion.VERSION_1_8
        targetCompatibility JavaVersion.VERSION_1_8
    }
    
    kotlinOptions {
        jvmTarget = '1.8'
    }
    
    // TFLite 설정
    aaptOptions {
        noCompress "tflite"
    }
}

dependencies {
    // Android Core
    implementation 'androidx.core:core-ktx:1.12.0'
    implementation 'androidx.appcompat:appcompat:1.6.1'
    implementation 'com.google.android.material:material:1.11.0'
    implementation 'androidx.constraintlayout:constraintlayout:2.1.4'
    
    // Activity Result API
    implementation 'androidx.activity:activity-ktx:1.8.2'
    
    // RecyclerView
    implementation 'androidx.recyclerview:recyclerview:1.3.2'
    
    // TensorFlow Lite
    implementation 'org.tensorflow:tensorflow-lite:2.14.0'
    implementation 'org.tensorflow:tensorflow-lite-support:0.4.4'
    
    // TensorFlow Lite GPU (선택)
    implementation 'org.tensorflow:tensorflow-lite-gpu:2.14.0'
    
    // 이미지 처리
    implementation 'androidx.camera:camera-core:1.3.1'
    implementation 'androidx.camera:camera-camera2:1.3.1'
    implementation 'androidx.camera:camera-lifecycle:1.3.1'
}
```

---

### 2. AndroidManifest.xml

```xml
<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- 권한 -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    
    <!-- 카메라 필수 -->
    <uses-feature android:name="android.hardware.camera" android:required="false" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="SCANEAT"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SCANEAT"
        tools:targetApi="31">
        
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
        <!-- FileProvider -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
        
    </application>

</manifest>
```

---

### 3. res/xml/file_paths.xml

```xml
<?xml version="1.0" encoding="utf-8"?>
<paths xmlns:android="http://schemas.android.com/apk/res/android">
    <external-files-path name="my_images" path="/" />
</paths>
```

---

### 4. assets 폴더 구조

```
app/src/main/assets/
├── best_int8.tflite    ← TFLite 모델 (6-8MB)
└── labels.txt          ← 클래스 라벨
```

**labels.txt 예시:**
```
Bokkeum_Dakgalbi
Bokkeum_DriedShrimpBokkeum
Bokkeum_DriedSquidBokkeum
...
(44개 클래스)
```

---

### 5. res/drawable/ (버튼 스타일)

**button_gradient.xml:**
```xml
<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <gradient
        android:startColor="#667eea"
        android:endColor="#764ba2"
        android:angle="135" />
    <corners android:radius="12dp" />
</shape>
```

**rounded_corner.xml:**
```xml
<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="#FFFFFF" />
    <corners android:radius="12dp" />
    <stroke android:width="1dp" android:color="#DDDDDD" />
</shape>
```

---

## 🚀 실행 방법

### 1. TFLite 모델 준비
```bash
# Google Colab에서 변환한 best_int8.tflite 다운로드
# Android Studio 프로젝트의 assets 폴더에 복사
```

### 2. labels.txt 생성
```python
# Python에서 생성
model = YOLO('best.pt')
with open('labels.txt', 'w') as f:
    for name in model.names.values():
        f.write(name + '\n')
```

### 3. Android Studio에서 빌드
```
File → Sync Project with Gradle Files
Build → Make Project
Run → Run 'app'
```

---

## 📊 성능 최적화

### GPU 사용 (더 빠름)
```kotlin
import org.tensorflow.lite.gpu.GpuDelegate

val options = Interpreter.Options().apply {
    addDelegate(GpuDelegate())
}
tfliteInterpreter = Interpreter(tfliteModel, options)
```

### NNAPI 사용 (NPU 지원)
```kotlin
import org.tensorflow.lite.nnapi.NnApiDelegate

val options = Interpreter.Options().apply {
    addDelegate(NnApiDelegate())
}
```

---

## 🐛 트러블슈팅

### 1. 모델 로드 실패
```
해결: assets 폴더에 best_int8.tflite 있는지 확인
```

### 2. 권한 에러
```
해결: AndroidManifest.xml에 권한 추가
      런타임 권한 요청 구현
```

### 3. 메모리 부족
```
해결: 이미지 리사이즈 크기 줄이기
      INPUT_SIZE = 640 → 416
```

### 4. 느린 추론 속도
```
해결: GPU Delegate 사용
      또는 더 작은 모델 (yolov8n) 사용
```

---

## 📱 APK 빌드

```bash
# Release APK
Build → Generate Signed Bundle / APK
→ APK 선택
→ 키스토어 생성/선택
→ Release 선택
→ Build

# 결과: app/release/app-release.apk
```

---

## 🎯 다음 단계

1. ✅ 기본 앱 완성
2. 🔄 칼로리 계산 API 연동
3. 🎨 UI/UX 개선
4. 📊 통계 기능 추가
5. 🔐 사용자 계정 시스템