# ── YOLO Segmentation ──────────────────────────
ultralytics>=8.0.0

# ── Depth Estimation (DPT) ─────────────────────
transformers>=4.35.0
timm>=0.9.0

# ── Image Processing ───────────────────────────
opencv-python>=4.8.0
numpy>=1.24.0
Pillow>=10.0.0

# ── 모델 Export (ONNX) ─────────────────────────
onnx>=1.14.0
onnxruntime-gpu>=1.16.0
# onnxruntime>=1.16.0  # CPU 환경

# ── 유틸 ────────────────────────────────────────
tqdm>=4.65.0
matplotlib>=3.7.0
PyYAML>=6.0

# ──────────────────────────────────────────────
# ⚠️ torch / torchvision은 CUDA 버전에 맞게 별도 설치 필요
# nvidia-smi 로 CUDA 버전 확인 후 아래 중 하나 실행
#
# CUDA 11.8:
# pip install torch torchvision --index-url https://download.pytorch.org/whl/cu118
#
# CUDA 12.1:
# pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
#
# CUDA 12.4:
# pip install torch torchvision --index-url https://download.pytorch.org/whl/cu124
# ──────────────────────────────────────────────